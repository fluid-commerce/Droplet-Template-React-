# Fluid Droplet Template - Frontend Environment Variables

# Backend API Configuration
# Your droplet's backend URL (change this to your deployed backend domain)
VITE_API_BASE_URL=https://droplet-backend.onrender.com

# Fluid Platform Configuration
# Your droplet's backend URL (not Fluid's platform - this is YOUR droplet service)
VITE_FLUID_API_URL=https://droplet-backend.onrender.com
VITE_FLUID_ENVIRONMENT=production

# Optional: Custom configuration
VITE_APP_NAME=Fluid Droplet Template
VITE_APP_VERSION=1.0.0

# Note: End users will provide their own Fluid API key in the configuration form
# This is just for frontend configuration, not user credentials
